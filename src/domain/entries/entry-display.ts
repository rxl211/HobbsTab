import type { FlightPurpose } from "./entry-types";

export const flightPurposeLabel: Record<FlightPurpose, string> = {
  hobby: "Fun / hobby",
  training: "Flight training",
  checkFlight: "Check flight",
};
